name: HelloWorld #name
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: |
          echo "A Demo build"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Phase
        run: |
          echo "Running the Testing"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Phase
        run: |
          echo "Deploy Phase is working"
